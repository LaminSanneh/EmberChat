export function initialize(container, application) {
  // application.inject('route', 'socketService', 'service:socket');
  // application.inject('controller', 'socketService', 'service:socket');
}

export default {
  name: 'socket-service',
  initialize: initialize
};
