import { shortenString } from '../../../helpers/shorten-string';
import { module, test } from 'qunit';

module('Unit | Helper | shorten string');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shortenString(42);
  assert.ok(result);
});
